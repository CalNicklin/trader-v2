# Insight Review — 2026-04-17

## Snapshot
- Window: trades 2026-03-18 → 2026-04-17 (30d); insights 2026-02-17 → 2026-04-17 (60d); snapshot generated 2026-04-17 07:53 UTC, VPS SHA `4cae34c`
- Insights reviewed: 154 unacted, 0 acted-on (Section 5 empty — no prior learning-loop action this window)
- Active strategies: 4 paper (`news_sentiment_mr_v1` #1, `earnings_drift_v1` #3, `sentiment_momentum_v1` #4, `earnings_drift_aggressive_v1` #5); 1 retired (`#2` killed 2026-04-08 on 15.92% drawdown)
- Team: Risk, Alpha, Opp, Exec, Skeptic

## Ranked proposals

### #1 — Catalyst-veto on short entries (Alpha)
**Expected impact:** +25–50 bps/week of drag eliminated, conservative post-amendment (was +40–70); ~£450 of 30-day counterfactual losses on catalyst-ignored shorts.
**Evidence:** 9 high-confidence `catalyst_ignored / filter_failure` trade_review insights (#1, #14, #288, #290, #299, #300) plus pattern_analysis #8; all name the same failure — shorts fired over earnings beats, guidance raises, analyst upgrades. Strategy 2 kill event 2026-04-08 (Section 6) was driven by this mechanism. Realised losses in Section 3: NVDA -54, EQIX -51, DLR -37, NOW -21, NFLX -66, AMD -40, TSLA -26, JNJ -62/-67.
**Implementation sketch:** new `src/strategies/filters/catalyst-veto.ts` (`shouldBlockShort`/`shouldBlockLong` with catalyst-polarity == move-direction check per Alpha R1); wire at entry gate in paper/executor; require structured `catalyst_type` field on classifier output in `src/news/classifier.ts`; env kill-switch `CATALYST_VETO_ENABLED` default `false` for staged rollout; fail-closed on classifier error (Risk cross-take amendment).
**Reversibility:** code with env kill-switch.
**Skeptic's objection:** R1 — blanket veto kills gap-fade wins (AAPL +£93.84) and inverts earnings_drift longs. R2 — the 30-day offline counterfactual is itself drawn from a single AI-rally regime; tuning veto windows/catalyst lists to it is regime-fit.
**Proposer response:** R1 — amended with polarity check (veto only when catalyst polarity matches price-move direction, preserves gap-fade overreactions), revised sizing down, added `CATALYST_VETO_ENABLED=false` default + 30-day offline counterfactual gate + fail-closed. R2 — not yet rebutted (Round 2 closed before proposer re-round); lead accepts the sub-window test requirement below.
**Lead verdict:** Highest single-change P&L lever in the review. The original blanket-veto would have been a mistake; the polarity-gated + env-gated + counterfactual-gated version is defensible. Ranked #1 because (a) it attacks the root cause the kill, the basket cap, and the stop-loss floors are all downstream mitigations for; (b) evidence is the deepest in the snapshot (9 conf-≥0.82 insights all saying the same thing); (c) shipping gated means being wrong costs nothing — the env flag stays off until the counterfactual passes. **Lead amendment (addressing Skeptic R2):** require the offline counterfactual to clear BOTH the first-15-day and last-15-day sub-windows independently, not just pooled. If parameters diverge by >15pp net-£ between halves, ship with tighter thresholds than the pooled optimum.

### #2 — Shared `hasStableEdge` sample-quality predicate governing graduation AND expectancy-kill (Alpha #2 + Risk #2, merged by lead)
**Expected impact:** Prevents two failure modes simultaneously — (a) lucky-regime promotion of strategy 3 (48.5 Sharpe at n=3, ~£200 of implied over-sizing risk at graduation); (b) ~20–40 bps/month of continued bleed on strategies 1 and 4 (Sharpe -13.9 / -15.6) that never trip the 15% drawdown kill.
**Evidence:** Strategy 3 Section 2 (100% win rate, n=3, all in 2026-04-08/14 AI-rally); strategy 3 insight #293 (conf 0.95, "fundamentally inverted") self-contradicts insight #292 ("clean_profit") from same review — a disagreement signal. Strategies 1/4 have `Sharpe<-13, profit_factor<0.15` but max_drawdown <2% so current kill rule never fires. Section 6 shows 1 kill / 30d — system is under-killing, not over-killing.
**Implementation sketch:** one shared predicate `hasStableEdge(strategy, direction: 'promote' | 'retire')` in `src/evolution/population.ts`, returning `false` unless `sampleSize ≥ 15 AND back-half-Sharpe sign matches full-sample Sharpe sign`. `src/strategy/graduation.ts` consumes for promote-gate; new `checkExpectancyKill()` consumes for kill-gate (kills when `hasStableEdge(s,'retire') == true AND Sharpe < -2 AND expectancy < 0`). `src/evolution/mutation-validator.ts` resets `sample_size` to 0 on signal-direction-affecting parameter changes. Constants: `EXPECTANCY_KILL_SHARPE_FLOOR=-2`, `MIN_SAMPLE_PROMOTE=15`, `MIN_SAMPLE_RETIRE=20` (floor raised per Risk R1 amendment).
**Reversibility:** config + one predicate function.
**Skeptic's objection:** On Alpha #2 — "no substantive objection; evidence and sizing are tight" (self-cited as the conservative-variance take). On Risk #2 — R1 regime-concentration critique (10-trade kill in one regime); resolved by Alpha's shared-predicate amendment which raises `MIN_SAMPLE_RETIRE` to 20 and requires regime stability. No R2 objection.
**Proposer response:** Merger was Alpha's R1 cross-take and Risk's R1 concession converging on the same machinery. Concede the regime sub-point and lift sample floor from 10 → 20 on retire side.
**Lead verdict:** Zero-cost hygiene that protects against both over-promotion and under-killing with one piece of machinery. Ranked #2 because it's a prerequisite for anything else that changes population composition (Risk #1/#4, Alpha #1 residual, Opp #1 seed). Ship before anything that kills/pauses a strategy.

### #3 — `MAX_CONCURRENT_POSITIONS` basket-cap with reject-full-tick semantics (Risk)
**Expected impact:** 4–6 bps of account equity saved per basket event (monthly base rate = 1 observed); preserves strategy-lives by keeping drawdown under 15% kill threshold on the next strategy 2-style failure.
**Evidence:** Section 3 at 2026-04-08 13:48:01.58–79Z — strategy 2 opened 7 concurrent shorts (AMD, META, TSLA, NVDA, GOOGL, AVGO, HSBA earlier at 07:23Z) within ~200ms. All 7 force-closed 2026-04-09 20:38:14 at uniform losses -£4.26 to -£9.43. Constant `MAX_CONCURRENT_POSITIONS=3` already declared in `src/risk/constants.ts` but paper loop does not consult it.
**Implementation sketch:** `src/paper/manager.ts` entry path — before recording an `entry_*` trade, count strategy's open positions; if opening this tick would breach the cap, **reject the entire tick** for that strategy (not 3-of-N), log `basket_over_cap` event. Three `basket_over_cap` events within 72h auto-promote to Risk #4's 48h paused flow. Keep the base-3 cap as second-order check for multi-tick accumulation.
**Reversibility:** code, purely additive guard clause reusing existing constant.
**Skeptic's objection:** R1 — arbitrary insert-order admission of 3-of-7 is risk-obfuscation, not risk reduction; the basket-trigger diagnostic is the real signal. No R2 (satisfied by reject-full-tick amendment).
**Proposer response:** Conceded core point. Amended to reject-full-tick rather than admit subset; tied to Risk #4's pause flow; dropped the tangential insight #11 claim about execution-zero-PnL.
**Lead verdict:** The single clearest event in the 30-day window (the 7-symbol basket) maps to a constant that was defined but not wired up. This is fixing a known bug, not adding a feature. Paired with Risk #4 it forms a coherent containment layer. Ranked #3 behind Alpha #3 (catalyst-veto attacks root cause) and the shared predicate (prerequisite for population changes).

### #4 — News-research calibration log (Exec #4b, measurement-only)
**Expected impact:** Zero direct P&L. Unblocks rational sizing on Alpha #4 (#9 below) and Opp #1 seed (#11 below) — both depend on an unfiltered confidence-bucketed precision of news_research. Saves the review cycle from shipping catalyst-momentum strategies on survivorship-filtered data.
**Evidence:** Section 9: `news_research` = 798 calls / $7.14 / 30d — top cost line. Section 4 missed_opportunity rows are by construction a filtered-winner set; no visibility into wrong-direction or null-move predictions from the same 798 calls. Skeptic R1 objections on Alpha #4, Opp #1, Opp #3 all converge on "produce the denominator."
**Implementation sketch:** new `research_outcome` table via `drizzle/migrations/NNNN_research_outcome.sql` — columns: `classification_id`, `confidence`, `predicted_direction`, `symbols`, `realised_move_24h`, `realised_move_48h`. Batch job `src/news/research-calibration.ts` runs T+2d, joins `news_research` against `quotes`, writes realised moves. No gating, no behaviour change, pure telemetry.
**Reversibility:** drop-table migration.
**Skeptic's objection:** R1 on original Exec #4 (gating version) — "cannot cap and accelerate simultaneously, conflicts with Opp/Alpha proposals." The #4b replacement has no R1 objection as a standalone measurement item; R2 flagged only the *bundled* co-ship with Alpha #4 (see #9 below), not #4b alone.
**Proposer response:** Withdrew the gating version entirely in favour of measurement-only. Concession on arbitrary 0.70/0.40 confidence thresholds.
**Lead verdict:** Ranked #4 because it is the **non-negotiable prerequisite** for Alpha #4 and Opp #1's strategy seed. Shipping it now, alone, sets up the October (post-calibration) decision about which catalyst-consuming strategy to seed. Cheap, cannot be wrong, staged ahead of every catalyst-long proposal in this review.

### #5 — LSE same-symbol BUY cooldown (Exec #1b)
**Expected impact:** ~£6–£10/week of preserved capital observed; saves 60–150 bps per blocked LSE round-trip. Defends live-trading friction envelope once `LIVE_TRADING_ENABLED=true`.
**Evidence:** Section 3 lines 73–77 — strategy 2 cycled HSBA three times same session 2026-04-08, each round-trip paying ~£9.34 friction (£8.01 stamp on BUY + £1.33 commission on SELL) = ~36% of each realised loss (-£25.81, -£23.53). LSE stamp duty 0.5% asymmetric on BUY.
**Implementation sketch:** `src/paper/executor.ts` (and `src/broker/executor.ts` live) — reject any LSE BUY if same strategy executed a BUY on same symbol within prior 4 hours, via query against `trades` table. Config `LSE_SAME_SYMBOL_BUY_COOLDOWN_HOURS` default 4, set 0 to disable. No `expected_edge` dependency.
**Reversibility:** pure config.
**Skeptic's objection:** R1 on original Exec #1 — `expected_edge` denominator is unaudited, penalises honest seeds. No R2 (satisfied by cooldown redesign).
**Proposer response:** Full concede; rebuilt the rule around observable broker state.
**Lead verdict:** Small but clean. Defends current HSBA cycling AND any future LSE same-symbol repeat pattern. Ranked #5 ahead of stops and seed proposals because it's a narrow, evidenced, observable-state rule with no knock-on — exactly the safe fix profile.

### #6 — Hard -5% per-trade kill floor (Risk #3, lead-amended to flat rule)
**Expected impact:** Caps tail-loss events (NFLX -66.95, JNJ -67.65, JNJ -62.30) at a known % of entry notional; ~10–15 bps/month of avoided tail-drawdown. Preserves the MRVL +114.37 / AVGO +82.71 winners (14.7% / 11.5% moves never touched -5% from entry).
**Evidence:** Section 3 tail losses all exceed -5%; strategy 3's three winners all moved ≥+10% without breaching -5%. Insight #14 (`stop functioned correctly, limiting losses to ~7.6%`) confirms stops work when present. Sections 7 mutation diffs show parents 1 and 3 were spawning with `stop_loss_pct=0`.
**Implementation sketch:** `src/evolution/spawner.ts` / `src/evolution/validator.ts` — reject any strategy whose `parameters.stop_loss_pct` is 0 or missing. `src/risk/` — add per-trade runtime kill at -5% from entry, applied uniformly regardless of strategy class. No class taxonomy.
**Reversibility:** config + one runtime guard.
**Skeptic's objection:** R1 on original — "1% / 2×ATR floor whipsaws drift winners." R2 on the class-aware amendment — "two-class dispatcher is infrastructure built for 4 strategies, and Opp/Alpha's new catalyst-driven longs fit neither class cleanly; simplify to flat -5% kill."
**Proposer response:** R1 conceded universal floor, amended to class-aware (2×ATR or 3% mean-reversion; 4×ATR or 8% drift). R2 not yet rebutted.
**Lead verdict:** Skeptic's R2 suggestion (flat -5% per-trade) is strictly better than the class-aware amendment for the current population size. It preserves Risk's defensible claim ("prevent tail losses >5%"), preserves MRVL/AVGO winners (the Skeptic R1 counterfactual), and sidesteps the taxonomy cost. **Lead amendment:** adopt flat -5% per-trade hard kill + non-zero stop_loss_pct on spawn, without a class taxonomy. Revisit class-aware only when population ≥6 strategies with varied hold horizons.

### #7 — Consecutive-loss + single-loss-5% circuit breaker (Risk #4)
**Expected impact:** 10–15 bps/month avoided drawdown; cheap defence-in-depth. Amended thresholds put false-pause rate near zero.
**Evidence:** Section 4 insight #8 (pattern_analysis conf 0.92) aggregated strategy 2's filter failure across 5 trades; Section 3 shows strategy 2 accumulated 3+ consecutive losing sequences before the kill fired; Section 6 confirms 1 kill in 30 days, zero soft-pause events — no "slow down" gear exists.
**Implementation sketch:** `src/evolution/population.ts` — new `checkConsecutiveLossPause()`; pause strategy 48h on EITHER (a) ≥5 consecutive losing exits OR (b) any single realised loss ≥5% of strategy virtual balance OR (c) ≥1 pattern_analysis insight conf≥0.85 with tag `filter_failure` or `recurring_failure`. Reuse `graduationEvents` with `event='paused'`. Constants: `CONSECUTIVE_LOSS_PAUSE=5`, `PAUSE_HOURS=48`.
**Reversibility:** config + one function + `paused` status enum value.
**Skeptic's objection:** R1 — 3-consecutive-loss threshold is inside normal variance (P(3|0.5)=12.5%); would have cut strategy 2 out of the AAPL +£93.84 win. Single-trade LLM reviewer self-contradicts (#292/#293) — unsafe to drive automated action. No R2.
**Proposer response:** Both sub-points conceded. Threshold raised 3→5 (P(5|0.5)=3.1%); bad-signal trigger restricted to pattern_analysis insights conf≥0.85, not individual trade_reviews. Added -5% single-loss secondary trigger (pairs with #6).
**Lead verdict:** Risk's own ranking puts this lowest of their four, which is correct. Defence-in-depth for the next strategy-2-style failure at a false-pause rate near zero. Ranked #7 behind the direct mitigations above.

### #8 — USO universe addition (Opp #2)
**Expected impact:** Zero direct P&L; one quote-poll cycle. Gives the desk an instrument to trade a future commodity catalyst (OPEC, inventory, geopolitical) if one fires.
**Evidence:** Section 4 insight #235 — USO -7.8% on Iran-ceasefire event. The one symbol where `commodity_price_impact` tag has an unambiguous mechanical link to the underlying. Zero energy coverage currently.
**Implementation sketch:** single-line universe add in `src/strategy/seed.ts`. No strategy seed.
**Reversibility:** config.
**Skeptic's objection:** R1 on original 7-symbol proposal — n=1 event, weak MU/WDC causal chain, LSE stamp-duty on SHEL/BP conflicts with Exec #1b. R2 — "no substantive objection; evidence and sizing are tight" (explicit).
**Proposer response:** Mostly conceded. Dropped SHEL/BP (LSE friction), dropped MU/WDC (weak causal chain), dropped the strategy seed, kept only USO. XOM/CVX deferred pending ≥3 independent event clusters in 90d backfill.
**Lead verdict:** Smallest possible footprint; Skeptic explicitly cleared it. Useful option value. Adopt.

### #9 — Catalyst-momentum long seed `catalyst_momentum_long_v1` (Alpha #4), staged behind #4
**Expected impact:** +10–25 bps/week net of friction if calibration precision ≥0.55 at conf≥0.85 on the five eligible catalyst types (revised from +40 bps gross per Exec cross-take friction correction).
**Evidence:** 17+ missed_opportunity insights at conf 0.78–0.98 describing correctly-directioned catalyst moves (#193 ANET +9.5%, #214 INTC +17.4%, #213 MRVL +10%, #82 AVGO +11.5%, #274 CRWV +14.7%, plus peer/supply-chain cluster). Section 9: 798 news_research calls / $7.14 / 30d producing structured research that is not being consumed by any strategy.
**Implementation sketch:** new seed `src/strategies/seeds/catalyst_momentum_long_v1.ts`; entry `predicted_direction='long' AND confidence≥0.85 AND catalyst_type IN (analyst_upgrade, strategic_partnership, major_contract, earnings_beat_with_guidance_raise, pricing_power_increase)`. 2-day hold, 3% target / 2% stop, 1–2% sizing (half the original proposal per Skeptic R2). **US-only v1** (Exec cross-take — avoids LSE stamp). `signal_polarity='momentum'` baked in to prevent evolution inverting it.
**Reversibility:** code + new strategy row (retire + seed-file delete to unwind).
**Skeptic's objection:** R1 — survivorship bias (missed_opportunity is winner-filtered). R2 — calibration sample (~80–150 evaluable predictions) may be regime-concentrated; per-catalyst-type n<30 is uninformative; bundled co-ship with Exec #4b creates a circular loop where the calibration log is populated by the same system it measures.
**Proposer response:** Full concede on R1 — added calibration prerequisite gate with thresholds (≥0.55 → 1% size, 0.40–0.55 → 0.5% size, <0.40 → don't ship seed). R2 bundling circularity not yet rebutted.
**Lead verdict:** This is the highest upside new strategy in the review, but R2 points to a genuine methodological trap in bundling with #4b. **Lead amendment (addressing Skeptic R2):** stage temporally — ship Exec #4b (Proposal #4) first for ≥30d of forward calibration accumulation, THEN make the Alpha #4 sizing decision. Require sub-window precision test (first 15d vs last 15d) to pass before the seed graduates paper → probation. If per-catalyst-type evaluable n<20, launch narrower on the two richest catalyst categories (`analyst_upgrade`, `strategic_partnership`) only. Ranked #9 because the P&L upside is real but the prerequisite chain is long and not in this review's scope to short-circuit.

### #10 — AI supply-chain universe-add gated to isolated per-strategy slice (Opp #1, lead-amended)
**Expected impact:** Universe side alone: optionality value (enables catalyst-momentum-long proposals above to act on the cluster). Strategy seed side: withdrawn to the same calibration-gated staging as Alpha #4.
**Evidence:** 40+ missed_opportunity insights cluster on AVGO, MRVL, ASML, TSM, AMAT, KLAC, LRCX, SMCI, ANET, DELL, INTC. Biggest single forgone-P&L lane; classifier is already tagging these symbols at volume (Section 8).
**Implementation sketch:** add the 11 AI supply-chain symbols to a named universe slice in `src/strategy/seed.ts` (new field e.g. `ai_supply_chain_universe`) that is consumed ONLY by catalyst-long seeds (per Skeptic R2 — do NOT expose to strategies 1 and 4). Strategies 1/4 retain their current universe boundary. `ai_supply_chain_tailwind_v1` seed remains withdrawn pending Exec #4b calibration.
**Reversibility:** config.
**Skeptic's objection:** R1 — survivorship-biased edge claim. R2 — universe-add exposes broken strategies 1/4 to 11 new high-beta AI names at AI-rally peak.
**Proposer response:** R1 conceded; split into universe-add now + calibration-gated seed later. R2 not yet rebutted.
**Lead verdict:** Skeptic R2 is correct and addressable by scoping the universe add to an isolated slice. **Lead amendment:** add the 11 symbols to an `ai_supply_chain` universe slice that only new catalyst-long seeds can consume. Strategies 1 and 4 keep their current universe until Alpha #3 catalyst-veto ships (#1 above). Ranked #10 because it depends on #1 (veto protects 1/4 from same-universe exposure) and #4 (calibration unlocks the seed). As a standalone universe-slice add, it is cheap and reversible.

### #11 — Pause (not retire) strategy 4 + add `signal_polarity` seed parameter (Alpha #1 residual)
**Expected impact:** Small. Recovers one strategy slot currently running at -15.6 Sharpe over 2 trades. Adds a tunable that mutation can explore under the #2 predicate.
**Evidence:** Strategy 4 Section 2 — Sharpe -15.6, expectancy -30.84, n=2. Section 7 — parent 1 → child 4 mutation lowered `sentiment_threshold` 0.7→0.4, preserving the problematic signal logic.
**Implementation sketch:** set `strategies.status='paused'` on strategy 4 (reuses Risk #4's paused state); let it accumulate ≥8 more paper trades under the Alpha #3 veto before a hasStableEdge-driven retire decision. Add `signal_polarity` parameter (values: `contrarian` / `momentum`) to `news_sentiment_mr_v1` seed definition; no default flip.
**Reversibility:** one DB update + one file edit.
**Skeptic's objection:** R2 — killing strategy 4 on n=2 in the same AI-rally week as strategy 1's losses is indistinguishable from regime-unlucky; sets a bad precedent for future n-small seeds. Recommend pause, not retire.
**Proposer response:** R1 half-concede (withdrew population-level sign flip and retire-and-respawn; kept kill strategy 4 claim). R2 not yet rebutted.
**Lead verdict:** Accept Skeptic R2 in full — pause, don't retire. This aligns the handling of strategy 4 with the shared predicate (#2): don't make retire decisions at n=2. The `signal_polarity` tunable is a cheap piece of future optionality. Ranked #11 — low priority but defensible once you've adopted the pause-instead-of-retire framing.

### #12 — Kill-event instrumentation fields (Exec #3b)
**Expected impact:** Zero direct P&L. Produces the dataset needed to revisit staggered kills once live trading is on.
**Evidence:** Section 6 — 1 kill / 30d, insufficient base rate to size the original staggered-exit proposal; paper-slippage is modelled, not measured.
**Implementation sketch:** add `kill_fill_duration_ms` and `kill_legs_count` fields to `graduation_events` metadata in `src/risk/kill-switch.ts`. ~15 lines. No behaviour change.
**Reversibility:** trivial (remove two fields).
**Skeptic's objection:** R1 on original Exec #3 — n=1 paper event, modelled friction, complexity cost. No R2 on the #3b replacement.
**Proposer response:** Full concede on original; replaced with instrumentation-only.
**Lead verdict:** Measurement-only, cheap, unlocks a future decision without committing to it. Adopt.

## Killed proposals

- **Alpha #1 original — population-level seed sign-flip + retire-and-respawn** (task #6) — killed because Alpha R1 withdrew the sign-flip after Skeptic R1 showed MSFT +£7.16 and CRM +£27.39 short-cover wins disprove uniform inversion. Residual pause-strategy-4 + polarity-tunable work survives as #11 above.
- **Opp #3 — sector_peer_readthrough_v1 strategy seed** (task #14) — killed because proposer explicitly withdrew for this review cycle (Opp R1); defers behind Alpha #4 and retains the ≥3-peer filter as a future entry condition if Alpha #4 graduates.
- **Exec #2b — source-allowlist news pre-filter** (task #10) — killed (parked) because upside is $1–2/month at vocabulary-rot risk; Exec explicitly offered to park it until Opp/Alpha benchmarks exist.
- **Exec #3 original — staggered kill-exit scheduler** (task #13) — killed because proposer withdrew (Skeptic R1 n=1 + modelled-friction + complexity cost objections fully conceded). Replaced by #12 above (instrumentation).
- **Exec #4 original — research budget gating** (task #16) — killed because proposer withdrew (Skeptic R1 "cannot cap and accelerate simultaneously" objection conceded). Replaced by #4 above (calibration log).
- **Opp #1 strategy seed (`ai_supply_chain_tailwind_v1`)** — the strategy-seed half of Opp #1 is parked pending Exec #4b calibration; only the universe-slice add survives in #10.
- **Opp #2 strategy seed (`commodity_geopolitical_v1`)** — withdrawn by proposer; 6 of 7 original symbols dropped; only the USO universe add survives in #8.
- **Risk #3 class-aware stop taxonomy** — killed in favour of the simpler flat -5% per-trade kill floor (lead amendment in #6). The mandatory-non-zero-stop-on-spawn rule survives in simplified form.

## Open questions for Cal

- **Counterfactual ownership for Alpha #3.** The offline 30-day counterfactual (with sub-window split per lead amendment on #1) needs an owner and a cost estimate. It's the load-bearing input for catalyst-veto adoption. Is this a Claude Opus self-improvement PR task or a manual analyst run?
- **Strategy 1 disposition after #1 ships.** Alpha #1 residual pauses strategy 4 but leaves strategy 1 running. If Alpha #3 veto ships, strategy 1 should accumulate forward evidence under the veto. Agree with leaving it alive at current paper size, or do you want a harder action?
- **Exec #4b batch cadence.** The calibration job is T+2d per call at default. Do you want a weekly rollup report committed to `docs/insight-reviews/` as standing precision telemetry, or just the DB table?
- **Per-strategy universe slicing (#10).** The proposed `ai_supply_chain_universe` isolated slice is a new concept — does the existing seed system already support per-strategy universe fields, or does this require a small schema extension? Raised as a plan-stage question.
- **Paper vs live sequencing.** Several proposals (Risk #1, #6, #7) are explicitly more valuable once `LIVE_TRADING_ENABLED=true`. Is there an expected flip date that should govern review priority, or do we treat all ranking as paper-only?
